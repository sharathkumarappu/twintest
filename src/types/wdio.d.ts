/**
 * Global `browser` variable injected by the WebDriverIO test runner at runtime.
 * This declaration makes TypeScript aware of it across all test files.
 */
declare const browser: WebdriverIO.Browser;
